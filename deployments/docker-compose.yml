services:
  db:
    image: postgres:15
    container_name: delta_postgres
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
      POSTGRES_DB: delta_engine
    ports:
      - "5432:5432"